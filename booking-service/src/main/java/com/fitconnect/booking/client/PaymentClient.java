package com.fitconnect.booking.client; import org.springframework.cloud.openfeign.FeignClient; import org.springframework.web.bind.annotation.*; import java.math.BigDecimal; import java.time.LocalDateTime; @FeignClient(name="payment-service") public interface PaymentClient {@PostMapping("/api/payments") PaymentSnapshot pay(PaymentRequest r);@GetMapping("/api/payments/booking/{id}") PaymentSnapshot getByBooking(@PathVariable Long id);@PostMapping("/api/payments/{id}/refund") PaymentSnapshot refund(@PathVariable Long id); record PaymentRequest(Long bookingId,String bookingReference,Long userId,BigDecimal amount,String paymentMethod,String cardLastFour,String transactionId){} record PaymentSnapshot(Long id,String paymentReference,Long bookingId,BigDecimal amount,String paymentMethod,String status,LocalDateTime paymentDate){}
}
